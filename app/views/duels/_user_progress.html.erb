<%= tag.div id: "#{dom_id(user)}_progress" do %>
  <% user_state = duel.user_state(user) %>
  <%= duel_progress(correct_answers: user_state.correct_answers,
                    points: user_state.points,
                    answered_questions: user_state.submissions) %>
<% end %>