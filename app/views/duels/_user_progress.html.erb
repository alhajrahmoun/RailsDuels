<%= tag.div id: "#{dom_id(user)}_progress" do %>
  <%= duel_progress(correct_answers: duel.user_state(user).correct_answers,
                    points: duel.user_state(user).points,
                    answered_questions: duel.user_state(user).submissions) %>
<% end %>