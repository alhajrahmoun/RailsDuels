<ul class="list-group">
  <% duel.users.each do |user| %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <%= user.username %> (<%= user.level %>)
      <span class="badge bg-primary rounded-pill"><%= user.points %></span>
    </li>
  <% end %>
</ul>
